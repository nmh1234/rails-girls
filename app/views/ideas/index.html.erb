<p id="notice"><%= notice %></p>

<h1>Here's a dog</h1>
<body> Insert clever text here.
  <br>
  <br>
<table>
  <thead>
    <tr>


      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @ideas.each do |idea| %>
        <tr><%= idea.name %></tr>
        <br>

      <tr><%= image_tag idea.picture_url, width: '100%' if idea.picture.present? %></tr>
          <tr><%= idea.description %></tr>
          <br>
        <tr><%= link_to 'Show', idea %></tr>
        <tr><%= link_to 'Edit', edit_idea_path(idea) %></tr>
        <tr><%= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' } %></tr>
  <br>
  <br>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Idea', new_idea_path %>
